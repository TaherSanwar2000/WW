import { View } from "react-native";
import React from "react";
import Maps from "./Map";

const AddressInfo = () => {
  return (
    <View style={{ flex: 1 }}>
        <Maps />
    </View>
  );
};

export default AddressInfo;